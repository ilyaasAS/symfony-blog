{# <nav class="bg-gray-800 p-4">
    <div class="container mx-auto flex justify-between items-center">
        <a href="{{ path('app_home') }}" class="text-white text-xl font-bold">Mon Blog</a>

        <!-- Menu Burger -->
        <div class="md:hidden flex items-center">
            <button id="menu-toggle" class="text-white focus:outline-none">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path>
                </svg>
            </button>
        </div>

        <!-- Menu Principal -->
        <div class="hidden md:flex space-x-4">
            <a href="{{ path('app_home') }}" class="text-gray-300 hover:text-white">Accueil</a>
            <a href="{{ path('app_about') }}" class="text-gray-300 hover:text-white">À propos</a>
            <a href="{{ path('app_contact') }}" class="text-gray-300 hover:text-white">Contact</a>
        </div>
    </div>

    <!-- Menu Déroulant pour Mobile -->
    <div id="mobile-menu" class="hidden md:hidden">
        <a href="{{ path('app_home') }}" class="block text-gray-300 hover:text-white px-4 py-2" onclick="closeMenu()">Accueil</a>
        <a href="{{ path('app_about') }}" class="block text-gray-300 hover:text-white px-4 py-2" onclick="closeMenu()">À propos</a>
        <a href="{{ path('app_contact') }}" class="block text-gray-300 hover:text-white px-4 py-2" onclick="closeMenu()">Contact</a>
    </div>
</nav>

<script>
    // Définir la fonction closeMenu
    function closeMenu() {
        const mobileMenu = document.getElementById('mobile-menu');
        mobileMenu.classList.add('hidden');
    }

    document.addEventListener('DOMContentLoaded', function() {
        const menuToggle = document.getElementById('menu-toggle');
        const mobileMenu = document.getElementById('mobile-menu');

        // Fonction pour basculer le menu
        menuToggle.addEventListener('click', function() {
            mobileMenu.classList.toggle('hidden');
        });

        // Ferme le menu lors du chargement de la page
        window.addEventListener('load', function() {
            closeMenu();
        });
        
        // Réinitialise le menu lorsque la page est rechargée
        window.addEventListener('pageshow', function(event) {
            if (event.persisted) {
                closeMenu();
            }
        });
    });
</script> #}

<nav class="navbar">
    <div id="navbar-menu-button">
        <svg width="24" height="24" viewbox="0 0 24 24" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="">
            <line x1="4" x2="20" y1="12" y2="12"/>
            <line x1="4" x2="20" y1="6" y2="6"/>
            <line x1="4" x2="20" y1="18" y2="18"/>
        </svg>
    </div>

    <ul id="navbar-menu" style="left:-100%">
        <li>
            <a href="/">Accueil</a>
        </li>
        <li>
            <a href="/contact">Contact</a>
        </li>
        <li>
            <a href="/about">A propos</a>
        </li>
    </ul>
</nav>

<script>
    const menuButton = document.querySelector('#navbar-menu-button');

    menuButton.addEventListener('click', () => {
        const menu = document.querySelector('#navbar-menu');

        if (menu.style.left == "0%") {
            menu.style.left = "-100%";
        } else {
            menu.style.left = "0%";
        }
    })
</script>

